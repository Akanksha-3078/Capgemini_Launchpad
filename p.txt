Use the Hooks Setup agent to fix this. 

Scan all files inside the step-definitions/ folder and find every 
this.<property> reference used in the step definitions (this.page, 
this.context, etc.). Then create a single file at 
step-definitions/support/hooks.ts that:
- Launches one shared Playwright chromium browser in BeforeAll
- Creates a new BrowserContext and Page in a Before hook, assigned to 
  this.context and this.page, so every scenario gets a fresh page
- Closes the context/page in an After hook
- Closes the browser in AfterAll

Then check my Cucumber config (cucumber.js or the cucumber field in 
package.json) and confirm the require/import glob actually includes 
step-definitions/support/*.ts — not just step-definitions/*.steps.ts. 
If it's too narrow, fix the glob and show me the before/after.

After the hooks file and config are in place, run the 
apollo-lab-test.feature file again using the Execution and Reporting 
agent and confirm whether this.page is now defined and all 8 scenarios 
(4 examples × outline + other scenarios) reach the Apollo site and pass. 
Report back the pass/fail table and update the Test Case and RTM status 
rows accordingly.
